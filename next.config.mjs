/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Uncomment below for static export (GitHub Pages)
  // Note: This will disable API routes
  // output: 'export',
  // images: {
  //   unoptimized: true,
  // },
}

export default nextConfig